{
  "name": "reverse-proxy",
  "version": "0.0.0",
  "scripts": {
    "start": "traefik --configfile ./traefik.yaml",
    "start:prod": "traefik --configfile ./traefik.yaml"
  },
  "dependencies": {}
}

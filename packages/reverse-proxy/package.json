{
  "name": "reverse-proxy",
  "version": "0.0.0",
  "scripts": {
    "start": "traefik --configfile ./static-config.yaml",
    "start:prod": "traefik --configfile ./static-config.yaml",
    "build": "echo 'no build task'",
    "test": "echo 'no reverse-proxy test'"
  },
  "dependencies": {}
}

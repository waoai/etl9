kind: Stage
name: LogOutput
endpoint: "http://localhost:9105/logoutput"
description: Log Output to ETL9 Server
inputs:
  input:
    type: any
    progressive: true
builtin: "yes"
outputs:
  is_complete:
    type: boolean

{
  "name": "database-rest-api",
  "version": "0.0.0",
  "scripts": {
    "start": "POSTGRES_USER=postgres POSTGRES_PASS=\"\" POSTGRES_HOST=localhost POSTGRES_PORT=5432 POSTGRES_DB=etl9 postgrest ./postgrest.conf",
    "start:prod": "postgrest ./postgrest.conf",
    "test": "tape tests/**/*.test.js"
  },
  "devDependencies": {
    "axios": "^0.18.0",
    "cross-env": "^5.2.0",
    "tape": "^4.10.0"
  }
}

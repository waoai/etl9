{
  "name": "database-rest-api",
  "version": "0.0.0",
  "scripts": {
    "start": "POSTGRES_USER=postgres POSTGRES_PASS=\"\" POSTGRES_HOST=localhost POSTGRES_PORT=5432 POSTGRES_DB=etl9 PORT=9102 postgrest ./postgrest.conf",
    "start:prod": "postgrest ./postgrest.conf",
    "build": "echo 'no build task'",
    "test": "echo 'no rest api tests'"
  },
  "devDependencies": {
    "axios": "^0.21.1",
    "cross-env": "^5.2.0",
    "tape": "^4.10.0"
  }
}
